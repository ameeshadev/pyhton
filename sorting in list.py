# sorting a list:
# It sorting the list automatically assending,by default
list1=["paper","books","songs","study"]
list1.sort()
print(list1)

# sorting for descending order:
# we use keyword argument reverse=true
list1.sort(reverse=True)
print(list1)
